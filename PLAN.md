# 改进计划（PLAN）

> 2026-08-10 全面体检后列出。按优先级排序，每项含现状证据与验收标准。
> 体检环境：175.27.210.206 vocab-api active · 词库 10,991 词 · 例句 32,650 条 · 音频 37,735 条 · 页面外网 200

## P0 · 影响学习正确性

1. **例句同词同档重复（287 组）**
   - 现状：`sentence/ensure`（llm-api-v1）与 `sentence/trio`（llm-tier-v1）双通道各自写过 tier1 → `static_sentences` 按 tier 排序取前 3，导致同词显示两条"基础"、挑战句被挤掉
   - 方案：查询改为每 tier 只取一条（llm-tier-v1 优先）；存量 287 组去重（保留质量更好的一条）
   - 验收：`SELECT ... HAVING COUNT(*)>1` 归零；页面任意词三档不重样

2. **108 个零例句词**
   - 现状：顽固失败词（多为虚词从句档，校验多次未过）
   - 方案：人工精修批（我逐词撰写 + 校验器过检），或专用宽松生成
   - 验收：零例句词 = 0

3. **FSRS 日内 learning steps 被压扁**
   - 现状：页面把 `due` 截成日期（`.slice(0,10)`），新卡"10 分钟后再学"变成"明天"——learning steps 失效
   - 方案：卡片 `due` 存完整 datetime，到期判断按分钟级；今日队列内支持"当天稍后再见"
   - 验收：新卡评分 Again 后 1~10 分钟内重新出现在当日队列

## P1 · 体验完整性

4. **统计页缺失**（PRD §3.9 有设计未实现）：保持率分布、30 天负载预测、学习日历热力图、顽固词列表
5. **设置页缺失**：每日新词数、负载档位（30/100/130）、提醒时间、嗓音选择、保持率旋钮
6. **跟读评分升级**：当前为词命中率演示版且仅 Chrome 系支持 → 按 PRD §5.4 实现三子分（完整度/流利度/近似准确度）；Safari 显示能力缺失提示
7. **个性化例句音频不预热**：生成后立刻对文本做 TTS 预热，次日播放零延迟
8. **自动播放首次拦截引导**：检测到被拦截时显示一次性提示"点任意 ▶ 后自动连播将开启"

## P2 · 工程加固

9. **HTTPS 正式化**：主站证书已过期（TrustAsia 2026-07-10 到期），且 DNSPod 境外 webblock 阻断 Let's Encrypt 验证 → 需腾讯云续签证书或处理备案；测试期 IP+HTTP 不能进入生产
10. **学习数据同步**：localStorage 换浏览器即丢 → v1 用 iCloud/CloudKit 或服务器 `/sync`（已按"数据同步不迁移逻辑"定稿，见 PRD §6.3）
11. **凭证轮换**：GitHub token 与服务器密码均曾明文出现在聊天中 → 吊销重发 GitHub token、腾讯云重置服务器密码
12. **例句质量打磨批**：召回词硬织入导致部分基础句生硬（如 "Do not involve the floor."）→ LLM 复审批（自然度打分 + 低分重写）
13. **tier1 召回织入率 93%**（765 条未织入）：收紧或标注

## P3 · 产品化推进

14. **M1 iOS App 开工**：SwiftUI + SwiftData + swift-fsrs + SpeechAnalyzer + AVSpeechSynthesizer；词库从 data/lexicon_v0.1.sqlite 起包；设计已全部就绪
15. **词库扩充维度**：短语/搭配学习卡（take → take care of）
16. **v2 发音精评**：Azure/讯飞 GOP 音素级评分（服务器代理已预留）

---

**建议执行顺序**：P0-1/2 先清数据卫生 → P1-4/5 补页面完整性 → P2-9 证书（上生产前必须）→ M1 开工。
